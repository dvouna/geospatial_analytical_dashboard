# Environment
.env
.env.local
.env.*.local
venv/
env/
ENV/
.venv

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Streamlit
.streamlit/cache/
.streamlit/secrets.toml
streamlit_cache/

# Data files (optional - add your data directory)
data/uploads/

# Logs
*.log

# Testing
.pytest_cache/
.coverage
htmlcov/

# Temp files
*.tmp
*.temp

# Byte-compiled / optimized / DLLs
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
*.egg
*.egg-info/
dist/
build/

# Virtual environments
.env
.env.*
.venv/
venv/
ENV/
env/
env.bak/
venv.bak/

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage
.coverage
.coverage.*
.cache
.pytest_cache/
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Jupyter Notebook
.ipynb_checkpoints/
*.ipynb

# Editors
*.sublime-workspace
*.sublime-project

# mypy/pyre
.mypy_cache/
.pyre/

# node
node_modules/

# Secrets and keys
secrets.json
*.pem
*.key

# OS generated files
Thumbs.db
desktop.ini

# Optional: ignore local run files
.streamlit/

# ── Unused / one-off scripts (identified during Option A refactor audit) ───────

# Standalone data-inspection script — not imported by any app module
inspect_data.py

# One-off syntax verification script — superseded by pytest smoke tests
verify_syntax.py

# Original pytest bootstrap — superseded by tests/test_pages_import.py
test_setup.py

# Windows launch batch file — use `streamlit run app.py` directly
run_app.bat

# ── Generated / environment artefacts ─────────────────────────────────────────

# Cloud Run server identity file — auto-generated at deploy time
server_user_id.txt

# Root-level Streamlit config copy — canonical copy lives in .streamlit/config.toml
streamlit_config.toml

# ── Planning / dev-only markdown ──────────────────────────────────────────────
# These are working notes generated during development; not needed in the repo.

00_START_HERE_FIRST.txt
page_structure.md
improvements.md
task.md
TESTING_SETUP_COMPLETE.md
TEST_REPORT.md
VPS_SETUP.md
TESTING_GUIDE.md
QUICKSTART.md
START_HERE.md
CLOUD-RUN_DEPLOYMENT.md
column_harmonisation_updates.md
improvement_plan.md
security_plan.md
vps_deployment
east_england_local_authorities_lower_level.geojson

# ── AI cache and generated artefacts (contain user prompts + model code) ─────
# These files are written at runtime and must not be committed or shipped in
# Docker images. See also .dockerignore.
data/approved_insights.jsonl
data/semantic_cache.json

# ── Deployment configuration ──────────────────────────────────────────────────
# .env contains real credentials — never commit.
# .env.production is a secrets-free template and IS tracked in Git.
/.env
/.env.local
# Explicitly un-ignore the template so it stays in the repo:
!.env.production